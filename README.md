# T-Language-Interpreter
A small interpreter for the T programming language, built with Flex and Bison. It parses source code into an abstract syntax tree (AST) and executes programs through a tree-walking evaluator, supporting variables, arithmetic expressions, conditionals, functions, input, output, and return statements.
